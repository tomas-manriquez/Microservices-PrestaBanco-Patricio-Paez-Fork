server:
  port: 8761
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: ${EUREKA_SERVER_URL}
    env:
      - name: CONFIG_SERVER_LOCATION
      - value: ${CONFIG_SERVER_LOCATION}