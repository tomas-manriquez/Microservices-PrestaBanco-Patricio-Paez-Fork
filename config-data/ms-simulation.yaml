server:
  port: 0
spring:
  datasource:
    url: jdbc:postgresql://simulation-db-service:5436/simulation-db
    username: postgres
    password: admin
eureka:
  instance:
    preferIpAddress: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://eureka-server-service:8761/eureka/