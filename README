Importante!

Utiliza el archivo .env.example como referencia para llenar los valores de entorno directamente en el Jenkins,
se puede realizar yendo a Manage Jenkins > Configure System y en Global properties colocar las Environment variables.

También, llenar los .env para cada uno de los microservicios en la carpeta env.

Para ejecutar el pipeline, asegúrate de tener python.exe en el PATH, SonarQube y Docker desktop instalado y corriendo.

En Jenkins tener los plugin de SonarQube y EnvFile junto con su configuración.